const persService = require('../services/personagens.Service');
const mongoose = require('mongoose');

const findAllPersonController = async (req, res) => { const allPerson = await PersonService.findAllPerson();

}